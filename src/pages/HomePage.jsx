import { RestaurantFilters, RestaurantsCards } from "../components"

const HomePage = () => {
  return (
    <section>
      <RestaurantFilters />
      <RestaurantsCards />
    </section>
  )
}

export default HomePage
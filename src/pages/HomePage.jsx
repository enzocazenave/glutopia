import { RestaurantFilters, RestaurantsCards } from "../components"

export const HomePage = () => {
  return (
    <section>
      <RestaurantFilters />
      <RestaurantsCards />
    </section>
  )
}